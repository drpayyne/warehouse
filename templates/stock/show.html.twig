{% extends 'base.html.twig' %}

{% block title %}Stock{% endblock %}

{% block body %}
    <h1>Stock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Sku</th>
                <td>{{ stock.sku }}</td>
            </tr>
            <tr>
                <th>Branch</th>
                <td>{{ stock.branch }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ stock.stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stock_index') }}">back to list</a>

    <a href="{{ path('stock_edit', {'sku': stock.sku}) }}">edit</a>

    {{ include('stock/_delete_form.html.twig') }}
{% endblock %}
